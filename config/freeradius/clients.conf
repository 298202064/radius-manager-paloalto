# FreeRADIUS clients configuration
# 此文件由 Web 界面自动管理，手动修改会被覆盖
# 请通过 NAS客户端 页面添加/编辑/删除客户端

# Localhost (always present)
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}
